#include <stdio.h>
#include <cs50.h>
#include <string.h>

void lala(void);
int main(void)
{
    string name = get_string ("Como você chama?\n");    
    {
        int i = get_int (0);
        strcmp (name, "lala");
        return
        ;
        if (i == 0)
        {
        printf("Oi, %s. Te amo!\n", name);  
        }
        else
        {
        printf("Oi, %s.\n", name);
        }
    }

}
