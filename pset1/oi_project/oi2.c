#include <stdio.h>
#include <cs50.h>

int main(void)

{
    string name = get_string ("Como você chama?\n"); 
    {
        if (name == lala)
            ;
            printf("Oi, %s. Te amo!\n", name);
        else 
            printf("Oi, %s.\n", name);
    }
}
